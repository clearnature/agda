# NOTE: bump this comment to force a CI cache miss when build dependencies change
#       (the cache key includes hashFiles of this file, not individual .hs sources)
resolver: nightly-2026-05-27
compiler: ghc-9.14.1
compiler-check: match-exact

# Local packages specified by relative directory name.
packages:
- '.'
